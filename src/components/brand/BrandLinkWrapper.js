import styled from 'styled-components';

const BrandLinkWrapper = styled.ul`
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: flex-start;
`;

export default BrandLinkWrapper;
